{
  "name": "adanalyzer",
  "version": "1.0.0",
  "description": "AdAnalyzer — Proxy local para GAS sem CORS",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js",
    "build:studio": "cd studio-src && npm install && npm run build"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "express-rate-limit": "^8.1.0",
    "googleapis": "^172.0.0",
    "helmet": "^8.1.0",
    "node-cron": "^4.2.1",
    "node-fetch": "^2.7.0",
    "pg": "^8.22.0"
  }
}
